#include <bits/stdc++.h>

#define ll long long

using namespace std;

ll a[200000];

void solve(ll a[], int n) {
    ll ans = 0;
    int i = 1;
    while (i < n) {
        
    } 
    cout << ans << '\n';
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n; 
        cin >> n;
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }
        solve(a, n);`
    }
}
