#include <bits/stdc++.h>

using namespace std;



void solve(vector<string> &s, int R, int C) {
    

}



int main() {
    int T;
    cin >> T;
    while (T--) {
        int R, C;
        cin >> R >> C;
        vector<string> s(R);
        for (int i = 0; i < R; i++) {
            cin >> s[i];
        }
        solve(s, R, C);
    }
}
