#include <bits/stdc++.h>

using namespace std;


void get_scores(int a, int b) {
    cout << a - 1 << ' ' << b << '\n';
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        int a, b;
        cin >> a >> b;
        get_scores(a, b);
    }
}       
